package Properties.inheritance;

public class Main {
    public static void main(String[] args) {
     /*   Box box=new Box(12,22,33);
        Box box1=new Box(box);*/

        //BOth class have many variables
        //we can access variables of ref type i.e BoxWeight
        //But to access we have to initialize and the object(Box i.e parent class) doesn't have access to child class
        //so error
        //BoxWeight box2=new Box(2,3,4,9);
        //System.out.println(box2.l+" "+box2.weight);

    }
}
